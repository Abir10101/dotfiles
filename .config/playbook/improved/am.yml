---
- name: Install system packages
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Download AM installer
      ansible.builtin.shell: curl -sL https://raw.githubusercontent.com/ivan-hc/AM/main/INSTALL -o /tmp/AM-INSTALL
      args:
        creates: /usr/local/bin/am
      mode: '0755'
    - name: Install AM
      ansible.builtin.command:
        cmd: /tmp/AM-INSTALL
      args:
        creates: /usr/local/bin/am
      become: yes
    - name: Remove AM installer
      ansible.builtin.file:
        path: /tmp/AM-INSTALL
        state: absent
    - name: Check if AM binary exists
      ansible.builtin.stat:
        path: /usr/local/bin/am
      register: am_bin
    - name: Install AM packages
      ansible.builtin.command:
        cmd: am -i "{{item}}"
      loop:
        - bruno
        - helix
        - librewolf
        - localsend
        - obs-studio
        - smplayer
        - yazi
        - zen-browser
      when: am_bin.stat.exists
